"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var React = __importStar(require("react"));
var useBreakpoint_1 = __importDefault(require("./useBreakpoint"));
var useSlider = function (_a) {
    var _b = _a.itemGap, itemGap = _b === void 0 ? 16 : _b, itemsPerPage = _a.itemsPerPage, _c = _a.maxContainerWidth, maxContainerWidth = _c === void 0 ? 960 : _c, _d = _a.defaultItemWidthMobile, defaultItemWidthMobile = _d === void 0 ? 230 : _d, _e = _a.defaultContainerPaddingMobile, defaultContainerPaddingMobile = _e === void 0 ? 16 : _e, _f = _a.defaultTotalPadding, defaultTotalPadding = _f === void 0 ? 48 : _f, _g = _a.defaultTotalPaddingMobile, defaultTotalPaddingMobile = _g === void 0 ? 24 : _g;
    var breakPoint = (0, useBreakpoint_1.default)();
    var _h = React.useState(0), currentItemIndex = _h[0], updateCurrentItemIndex = _h[1];
    var _j = React.useState(293), itemWidth = _j[0], updateItemWidth = _j[1];
    var _k = React.useState(), maxWidth = _k[0], updateMaxWidth = _k[1];
    var ipp = itemsPerPage[breakPoint];
    var switchSlide = React.useCallback(function (action) { return function () {
        updateCurrentItemIndex(function (n) { return (action === 'prev' ? n - 1 : n + 1); });
    }; }, []);
    var calculateItemWidth = React.useCallback(function () {
        if (breakPoint === 'xs') {
            updateItemWidth(defaultItemWidthMobile);
            return defaultItemWidthMobile;
        }
        var clientWidth = window.document.documentElement.clientWidth;
        // capture padding of container
        var paddingTotal = breakPoint === 'xs' ? defaultTotalPaddingMobile : defaultTotalPadding;
        // calculate space left for the carousel
        var containerWidth = clientWidth - paddingTotal;
        if (maxContainerWidth && clientWidth > maxContainerWidth) {
            containerWidth = maxContainerWidth - paddingTotal;
        }
        // calculate item width considering containerWidth and itemGap
        var currItemWidth = (containerWidth - (ipp - 1) * itemGap) / ipp;
        updateItemWidth(currItemWidth);
        return currItemWidth;
    }, [breakPoint, ipp, itemGap, defaultItemWidthMobile, defaultTotalPadding, defaultTotalPaddingMobile]);
    var calculateMaxWidth = React.useCallback(function (itemWidthParam) {
        var clientWidth = window.document.documentElement.clientWidth;
        if (breakPoint === 'xs') {
            updateMaxWidth(clientWidth - defaultContainerPaddingMobile);
            return;
        }
        updateMaxWidth(itemWidthParam * ipp + itemGap * (ipp - 1));
    }, [breakPoint, ipp, itemGap, defaultContainerPaddingMobile]);
    var calculate = React.useCallback(function () {
        var itemWidthParam = calculateItemWidth();
        if (itemWidthParam) {
            calculateMaxWidth(itemWidthParam);
        }
    }, [calculateItemWidth, calculateMaxWidth]);
    React.useEffect(function () {
        calculate();
        window.addEventListener('resize', calculate);
        return function () { return window.removeEventListener('resize', calculate); };
    }, [breakPoint]);
    return {
        currentItemIndex: currentItemIndex,
        itemWidth: itemWidth,
        maxWidth: maxWidth,
        switchSlide: switchSlide,
        updateCurrentItemIndex: updateCurrentItemIndex,
    };
};
exports.default = useSlider;
//# sourceMappingURL=useSlider.js.map